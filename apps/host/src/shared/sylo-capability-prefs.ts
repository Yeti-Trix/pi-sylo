/**
 * Sylo capability-manager pref keys — no Node built-ins so the renderer can import safely.
 */

/** When true, list/apply skills under the active workspace `.cursor/skills` only. */
export const SYLO_INCLUDE_CURSOR_SKILLS_PREF = 'sylo.include_cursor_skills' as const
