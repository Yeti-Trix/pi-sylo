# Think Tank route fallback

Enable **sylo-think-tank** in Capability manager and the **think-tank** skill. Rebuild the route UI (`npm run build:ui -w sylo-think-tank`) if this page persists.

Configure seat models in **Think Tank → Settings** (same provider/model options as **Settings → Model (Pi)**). Trigger debates from chat: `Think Tank: your topic here`.
