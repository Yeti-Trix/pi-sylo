# Web access

Enable **Web access** under Capability manager → Sylo optional packages, restart the broker, and run `npm run bootstrap-pi` so this route and the **web-access** skill appear.

Privacy-first search uses DuckDuckGo (S1). Rank and rewrite use your local Ollama model.
