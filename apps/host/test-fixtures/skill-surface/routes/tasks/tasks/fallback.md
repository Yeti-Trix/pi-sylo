# Tasks

The Tasks dashboard needs JavaScript. Enable JavaScript in this Sylo route, or
ask the agent to surface a list on the Canvas with `sylo_task_open_on_canvas`.

Task lists live at `<workspace>/.sylo/tasks.json` and are shared with the agent
via the `sylo_task_*` tools.