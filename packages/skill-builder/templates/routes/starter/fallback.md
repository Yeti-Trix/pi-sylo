# Route unavailable

This Sylo host does not expose persistent skill routes (`supports_route: false`), or required bridge capabilities were not negotiated.

Perform the same work through chat-only interaction: summarize what the sidebar UI would do, ask concise follow-ups, and use ordinary tools (`read_skill_data` / prompts) when needed.
