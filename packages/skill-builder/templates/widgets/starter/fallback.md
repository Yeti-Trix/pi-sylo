# Widget unavailable

This host cannot render skill widgets (`supports_widget: false`), negotiated capabilities are insufficient, or the widget failed sandbox checks.

Describe the same workflow in plain chat: summarize what the widget would collect, confirm values with the operator, then proceed using normal agent tools.
